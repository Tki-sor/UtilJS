package com.tkisor.utiljs.wrapper;

import net.minecraft.world.level.block.state.BlockState;

public class BlockStateWrapper extends Wrapper<BlockState> {
    public BlockStateWrapper(BlockState value) {
        super(value);
    }
}
