package com.tkisor.utiljs.wrapper;

import net.minecraft.core.BlockPos;

public class BlockPosWrapper extends Wrapper<BlockPos> {
    public BlockPosWrapper(BlockPos value) {
        super(value);
    }
}
